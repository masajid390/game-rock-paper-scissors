import { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
  borderRadius: "8px",

  colors: {
    main: "hsl(0, 0%, 100%)",
    secondary: "hsl(229, 25%, 31%)",
  },
};

export { theme };
